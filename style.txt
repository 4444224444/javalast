/* style.css */

body {
  margin: 0;
  overflow: hidden; /* 스크롤바 숨기기 */
  background-color: #000;
}

#canvas {
  display: block;
  width: 100vw;
  height: 100vh;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1;
}

#ui-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: 10;
  pointer-events: none;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  color: white;
}

/* 시작 화면 (클릭 오버레이) */
#start-screen {
  position: fixed;
  inset: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  z-index: 50;
  background: transparent;
  color: #fff;
  cursor: pointer;
  transition: opacity 0.8s ease;
}

/* 좌측 하단 메인 타이틀 */
/* 좌측 하단 메인 타이포 */
#title-box {
  position: fixed;
  left: 3rem;
  bottom: 0.5rem;
  color: #ffffff;
  pointer-events: none;
  display: flex;              /* 🔥 flex로 묶기 */
  flex-direction: column;     /* 세로 정렬 */
  gap: 0.5rem;                /* 🔥 둘 사이 간격 — 여기서 조절 */
}

.main-title {
  font-family: 'Orbitron';
  font-size: 8rem;
  font-weight: 700;
  left: 3rem;
  margin-bottom: 0;
  letter-spacing: -0.04rem;
  line-height: 0.85;
    /* 🔥 그라데이션 텍스트 */
  background: linear-gradient(to bottom, #7e7e7e 0%, #ffffff 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.main-sub {
  font-family: 'pretendard';
  font-size: 2rem;
  font-weight: 100;
  letter-spacing: -0.04rem;
  line-height: 1;
  background: linear-gradient(to bottom, #ffffff 0%, #7e7e7e 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-top: 0;
  margin-left: 20rem;
  white-space: nowrap; 
}

.start-hint {
  font-family: 'pretendard';
  font-size: 2rem;
  font-weight: 100;
  letter-spacing: -0.04rem;
  line-height: 1;
}


/* ===== HUD (거리 / 조작 안내 / 시계) ===== */

#hud {
  position: fixed;
  top: 2rem;
  left: 3rem;
  color: #ffffff;
  font-family: 'prentedard';
  font-size: 0.9rem;
  line-height: 1;
  pointer-events: none;
  opacity: 1;
  transition: opacity 0.4s ease;
}

#distance-label {
  margin-bottom: 0.2rem;
}

#controls-hint {
  margin-bottom: 0.2rem;
  opacity: 0.7;
}

/* 노드 클릭 시 HUD 스르륵 사라지는 효과 */
#hud.fade-out {
  opacity: 0;
}


/* ===== 좌측 하단 PARAM LAB (DEV CONSOLE) ===== */

/* ============================
   DEV CONSOLE / PARAM LAB
   ============================ */

#dev-console {
position: fixed;
left: 32px;
bottom: 80px;
width: 260px;
height: 220px;
padding: 10px 12px 12px;
box-sizing: border-box;

/* 🔎 흑백 실험실 콘솔 느낌 */
background:
radial-gradient(circle at top, rgba(15, 23, 42, 0.96), rgba(0, 0, 0, 0.98));
border: 1px solid rgba(248, 250, 252, 0.2);
box-shadow:
0 0 18px rgba(0, 0, 0, 0.95),
0 0 32px rgba(15, 23, 42, 0.9);

font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo,
Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
font-size: 10px;
color: #f9fafb;

z-index: 35;
opacity: 0;
transform: translateY(8px);
}

/* 외곽 코너 라인 (HUD 느낌 맞추기) */

#dev-console::before,
#dev-console::after {
content: "";
position: absolute;
pointer-events: none;
}

/* 안쪽 프레임 */

#dev-console::before {
inset: 4px;
border: 1px solid rgba(248, 250, 252, 0.08);
}

/* 상단 구분 라인 (CRT 수평선 느낌) */

#dev-console::after {
left: 0;
right: 0;
top: 26px;
height: 1px;
background: linear-gradient(
to right,
transparent,
rgba(248, 250, 252, 0.55),
transparent
);
}

/* 상단 제목 라인 */

.dev-header {
display: flex;
justify-content: space-between;
align-items: center;

letter-spacing: 0.18em;
text-transform: uppercase;
font-size: 9px;
color: rgba(229, 231, 235, 0.85);
margin-bottom: 6px;
}

/* 오른쪽에 시스템 코드 라벨 */

.dev-header::after {
content: "SYS/PLAB 01";
font-size: 8px;
opacity: 0.6;
}

/* 코드 입력 영역 */

#dev-input {
width: 100%;
height: 150px;
padding: 6px 7px;
box-sizing: border-box;
resize: none;

background-color: #020617;
border-radius: 4px;
border: 1px solid rgba(148, 163, 184, 0.7);
color: #f9fafb;

font-size: 10px;
line-height: 1.4;

/* CRT 그리드 + 스캔라인 느낌 */
background-image:
linear-gradient(
rgba(148, 163, 184, 0.12) 1px,
transparent 1px
),
linear-gradient(
90deg,
rgba(148, 163, 184, 0.10) 1px,
transparent 1px
);
background-size: 14px 14px, 14px 14px;
}

#dev-input:focus {
outline: none;
border-color: rgba(248, 250, 252, 0.9);
box-shadow: 0 0 0 1px rgba(248, 250, 252, 0.55);
}

/* 실행 버튼 */

#dev-run {
margin-top: 7px;
width: 100%;
height: 24px;
border-radius: 4px;

background: transparent;
border: 1px solid rgba(248, 250, 252, 0.55);
color: #f9fafb;

font-size: 9px;
letter-spacing: 0.16em;
text-transform: uppercase;
cursor: pointer;

transition:
background 0.15s ease,
color 0.15s ease,
border-color 0.15s ease,
transform 0.08s ease,
box-shadow 0.15s ease;
}

#dev-run:hover {
background: rgba(248, 250, 252, 0.06);
border-color: rgba(248, 250, 252, 0.9);
box-shadow: 0 0 14px rgba(248, 250, 252, 0.35);
transform: translateY(-0.5px);
}

#dev-run:active {
transform: translateY(0);
box-shadow: 0 0 6px rgba(248, 250, 252, 0.25);
}

/* 숨김 상태 그대로 유지 */

#dev-console.hidden {
opacity: 0;
pointer-events: none;
transform: translateY(8px);
transition: opacity 0.25s ease, transform 0.25s ease;
}

/* 공통: 처음에는 왼쪽 밖에서 대기 */

#hud,
#dev-console,
.fp-side-panel {
  opacity: 0;
  transform: translateX(-40px);
  pointer-events: none;
}

/* ===== 1인칭 게임 HUD 레이어 ===== */

/* =========================
   FP HUD OVERLAY
========================= */

#fp-ui {
  position: fixed;
  inset: 0;
  z-index: 50; /* 캔버스 위, 대시보드(999) 아래 */
  font-family: "Orbitron", system-ui, sans-serif;
  color: #f5f5f5;
  opacity: 0;
  pointer-events: none; /* HUD는 클릭 안 먹게 */
  transition: opacity 0.4s ease-out;
}

#fp-ui.active {
  opacity: 0.9;
}

.fp-bottom-bar,
.fp-bottom-chip,
.fp-music-toggle {
  pointer-events: auto;
}

/* 살짝 어두운 비네트 + 중심 집중 */
.fp-vignette {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at center,
      transparent 0%,
      rgba(0, 0, 0, 0.35) 70%,
      rgba(0, 0, 0, 0.85) 100%);
  mix-blend-mode: multiply;
}

/* 수평 스캔라인 */
.fp-scanline {
  position: absolute;
  inset: 0;
  background-image: linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.06) 1px,
    rgba(0, 0, 0, 0) 1px
  );
  background-size: 100% 3px;
  opacity: 0.22;
  mix-blend-mode: screen;
  animation: fpScanMove 8s linear infinite;
}

/* 중앙 에임 전체 래퍼 */
.fp-reticle {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 160px;   /* 원래 120 → 좀 더 크게 */
  height: 160px;
  transform: translate(-50%, -50%);
}

/* 둥근 링 */
.fp-reticle-ring {
  position: absolute;
  inset: 24px; /* 링도 살짝 키움 */
  border-radius: 999px;
  border: 1px solid rgba(229, 231, 235, 0.95);
  box-shadow:
    0 0 20px rgba(249, 250, 251, 0.9),
    0 0 40px rgba(249, 250, 251, 0.4);
  animation: fpReticlePulse 2.6s ease-in-out infinite;
}

/* 십자선 */
.fp-reticle-lines {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}

.fp-reticle-lines-h::before,
.fp-reticle-lines-h::after,
.fp-reticle-lines-v::before,
.fp-reticle-lines-v::after {
  content: "";
  position: absolute;
  background: rgba(243, 244, 246, 0.95);
  box-shadow: 0 0 12px rgba(249, 250, 251, 0.9);
}

/* === 여기서 선 길이 늘림 === */

/* 가로선: 좌우로 길게 */
.fp-reticle-lines-h::before,
.fp-reticle-lines-h::after {
  width: 120px;    /* 원래 32 → 길게 */
  height: 1px;
}

.fp-reticle-lines-h::before {
  left: -140px;    /* 화면 중앙 기준 더 밖으로 나가게 */
}
.fp-reticle-lines-h::after {
  right: -140px;
}

/* 세로선: 위아래로 길게 */
.fp-reticle-lines-v::before,
.fp-reticle-lines-v::after {
  width: 1px;
  height: 120px;   /* 원래 32 → 길게 */
}

.fp-reticle-lines-v::before {
  top: -140px;
}
.fp-reticle-lines-v::after {
  bottom: -140px;
}

/* 중앙 점 */
.fp-reticle-dot {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 6px;
  height: 6px;
  transform: translate(-50%, -50%);
  border-radius: 999px;
  background: #f9fafb;
  box-shadow:
    0 0 10px rgba(249, 250, 251, 1),
    0 0 20px rgba(249, 250, 251, 0.9);
}

/* 모서리 프레임 */

.fp-corner {
  position: absolute;
  padding: 8px 12px;
  font-size: 10px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: #e5e7eb;
  text-shadow: 0 0 4px rgba(107, 114, 128, 0.9);
}

.fp-corner::before,
.fp-corner::after {
  content: "";
  position: absolute;
  border-color: rgba(148, 163, 184, 0.8);
  border-style: solid;
}

/* 각 모서리별 위치 및 프레임 */

.fp-corner-tl {
  left: 24px;
  top: 20px;
}
.fp-corner-tl::before {
  left: -6px;
  top: -6px;
  width: 40px;
  height: 16px;
  border-width: 2px 0 0 2px;
}
.fp-corner-tl::after {
  left: -6px;
  top: 10px;
  width: 20px;
  height: 18px;
  border-width: 0 0 2px 2px;
}

.fp-corner-tr {
  right: 24px;
  top: 20px;
  text-align: right;
}
.fp-corner-tr::before {
  right: -6px;
  top: -6px;
  width: 40px;
  height: 16px;
  border-width: 2px 2px 0 0;
}
.fp-corner-tr::after {
  right: -6px;
  top: 10px;
  width: 20px;
  height: 18px;
  border-width: 0 2px 2px 0;
}

.fp-corner-bl {
  left: 24px;
  bottom: 22px;
}
.fp-corner-bl::before {
  left: -6px;
  bottom: -6px;
  width: 40px;
  height: 16px;
  border-width: 0 0 2px 2px;
}
.fp-corner-bl::after {
  left: -6px;
  bottom: 10px;
  width: 20px;
  height: 18px;
  border-width: 2px 0 0 2px;
}

.fp-corner-br {
  right: 24px;
  bottom: 22px;
  text-align: right;
}
.fp-corner-br::before {
  right: -6px;
  bottom: -6px;
  width: 40px;
  height: 16px;
  border-width: 0 2px 2px 0;
}
.fp-corner-br::after {
  right: -6px;
  bottom: 10px;
  width: 20px;
  height: 18px;
  border-width: 2px 2px 0 0;
}

.fp-corner-label {
  opacity: 0.9;
}

/* 좌우 사이드 패널 */

.fp-side-panel {
  position: absolute;
  top: 280px;
  left: 32px;
  width: 210px;

  padding: 15px 25px;
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  border-radius: 2px;

  /* 🔎 폰트: 실험실 HUD 느낌 */
  font-family: "Orbitron", system-ui, -apple-system, BlinkMacSystemFont,
    "Segoe UI", sans-serif;

  /* 🔎 흑백 + CRT 글로우 */
  border: 1px solid rgba(248, 250, 252, 0.2);
  background:
    radial-gradient(circle at top, rgba(15, 23, 42, 0.92), rgba(0, 0, 0, 0.98));
  box-shadow:
    0 0 16px rgba(0, 0, 0, 0.95),
    0 0 32px rgba(15, 23, 42, 0.9);

  color: #f9fafb;
  backdrop-filter: blur(6px);

  /* 세로 중앙 유지 */
  transform: translateY(-50%);

  /* 🔥 처음에는 안 보이게 */
  opacity: 0;
  pointer-events: none;

  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

/* HUD 패널 프레임 & 스캔라인 */

.fp-side-panel::before,
.fp-side-panel::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
}

/* 안쪽 얇은 프레임 */

.fp-side-panel::before {
  inset: 3px;
  border: 1px solid rgba(248, 250, 252, 0.06);
}

/* 아주 약한 수평 스캔라인 */

.fp-side-panel::after {
  inset: 0;
  background-image: linear-gradient(
    to bottom,
    rgba(148, 163, 184, 0.08) 1px,
    transparent 1px
  );
  background-size: 0.5px 4px;
  opacity: 0.45;
}

/* 제목 + 라인들 */

.fp-side-title {
  font-size: 9px;
  color: rgba(229, 231, 235, 0.88);
  margin-bottom: 6px;
  border-bottom: 1px solid rgba(148, 163, 184, 0.6);
  padding-bottom: 3px;

  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* 작은 시스템 코드 느낌 서브 텍스트 (옵션) */
.fp-side-title::after {
  content: "SCAN: NEURO/FP";
  font-size: 8px;
  letter-spacing: 0.18em;
  color: rgba(148, 163, 184, 0.8);
}

/* 각 정보 라인 */

.fp-side-line {
  display: flex;
  justify-content: space-between;
  margin-bottom: 2px;
  color: #e5e7eb;
}

.fp-side-line span:first-child {
  color: #a1a1aa;
}

/* 숫자 줄은 살짝 작게 (DEPTH, DIST용) */

.fp-side-line-small {
  font-size: 9px;
  opacity: 0.9;
}



/* ===========================
   NODE PROXIMITY HUD (독립)
   =========================== */

.node-proximity-hud {
  position: fixed;
  right: 32px;
  top: 50%;
  transform: translateY(-50%);

  min-width: 210px;
  padding: 10px 16px;

  font-family: "Orbitron", system-ui, -apple-system, BlinkMacSystemFont,
    "Segoe UI", sans-serif;
  font-size: 10px;
  letter-spacing: 0.18em;
  text-transform: uppercase;

  border-radius: 2px;
  border: 1px solid rgba(248, 250, 252, 0.2);
  background: radial-gradient(
    circle at top,
    rgba(15, 23, 42, 0.96),
    rgba(0, 0, 0, 0.98)
  );
  box-shadow:
    0 0 18px rgba(0, 0, 0, 0.95),
    0 0 32px rgba(15, 23, 42, 0.9);

  color: rgba(229, 231, 235, 0.95);
  backdrop-filter: blur(6px);

  opacity: 0;
  pointer-events: none;
  transition:
    opacity 0.25s ease,
    transform 0.25s ease;
}

/* HUD가 활성 상태일 때 */
.node-proximity-hud.visible {
  opacity: 1;
  transform: translateY(-50%) translateX(0);
}

/* 거리 단계별 상태 색 */

.node-proximity-hud.state-far {
  color: rgba(148, 163, 184, 0.9);
}

.node-proximity-hud.state-mid {
  color: rgba(248, 250, 252, 0.95);
}

.node-proximity-hud.state-near {
  color: #fbbf24;
}

.node-proximity-hud.state-hot {
  color: #f97373;
  animation: nodeProximityPulse 0.9s ease-in-out infinite;
}

@keyframes nodeProximityPulse {
  0% {
    opacity: 0.5;
    text-shadow: 0 0 4px rgba(248, 113, 113, 0.4);
  }
  50% {
    opacity: 1;
    text-shadow: 0 0 12px rgba(248, 113, 113, 0.9);
  }
  100% {
    opacity: 0.5;
    text-shadow: 0 0 4px rgba(248, 113, 113, 0.4);
  }
}




/* 하단 헬프 바 */
.fp-bottom-bar {
  position: absolute;
  left: 50%;
  bottom: 16px;
  transform: translateX(-50%);
  display: flex;
  gap: 10px;
  align-items: center;
  z-index: 50;
}

.fp-bottom-chip {
  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.9);
  background: rgba(0, 0, 0, 0.85);
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: #f5f5f5;
  box-shadow: 0 0 12px rgba(0, 0, 0, 0.9);
}

/* === MUSIC TOGGLE 확장 버전 === */

.fp-music-toggle {
  margin-left: auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  min-width: 70px;
  padding-inline: 12px;
  cursor: pointer;
  transition:
    border-color 0.25s ease,
    box-shadow 0.25s ease,
    background 0.25s ease;
}

/* 왼쪽에 BGM 라벨 */
.fp-music-toggle .music-label {
  font-size: 9px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  opacity: 0.7;
}

/* 오른쪽에 작은 이퀄라이저 박스 */
.fp-music-toggle .music-icon {
  display: inline-flex;
  align-items: flex-end;
  justify-content: center;
  gap: 2px;
  width: 20px;
  height: 10px;
}

/* 막대 기본 상태 */
.fp-music-toggle .music-icon .bar {
  --h: 4px; /* 기본 높이 */
  width: 2px;
  height: var(--h);
  border-radius: 999px;
  background: rgba(148, 163, 184, 0.7);
  transform-origin: bottom center;
  transform: scaleY(0.4);
  opacity: 0.4;
}

/* ON 상태일 때 버튼 전체 하이라이트 */
.fp-music-toggle[data-state="on"] {
  border-color: rgba(56, 189, 248, 0.9);
  box-shadow:
    0 0 10px rgba(56, 189, 248, 0.4),
    0 0 30px rgba(56, 189, 248, 0.2);
  background: radial-gradient(circle at 0% 0%, rgba(56, 189, 248, 0.18), rgba(0, 0, 0, 0.85));
}

/* ON 상태에서 라벨 살짝 밝게 */
.fp-music-toggle[data-state="on"] .music-label {
  opacity: 1;
}

/* ON 상태에서만 막대 애니메이션 */
.fp-music-toggle[data-state="on"] .music-icon .bar {
  opacity: 1;
  background: #38bdf8;
  animation: musicEqualize 0.9s ease-in-out infinite;
}

/* 각 막대 딜레이 줘서 물결처럼 */
.fp-music-toggle[data-state="on"] .music-icon .bar-1 {
  animation-delay: 0s;
}
.fp-music-toggle[data-state="on"] .music-icon .bar-2 {
  animation-delay: 0.12s;
}
.fp-music-toggle[data-state="on"] .music-icon .bar-3 {
  animation-delay: 0.24s;
}

/* 이퀄라이저 파동 */
@keyframes musicEqualize {
  0% {
    transform: scaleY(0.4);
  }
  20% {
    transform: scaleY(1.2);
  }
  40% {
    transform: scaleY(0.7);
  }
  60% {
    transform: scaleY(1.4);
  }
  80% {
    transform: scaleY(0.5);
  }
  100% {
    transform: scaleY(0.4);
  }
}





.keyword-label {
  position: fixed;
  transform: translate(-50%, -50%);
  font-size: 10px;
  font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo,
    Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  color: #e5f9ff;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  pointer-events: none;
  white-space: nowrap;

  /* 빛나는 느낌 */
  text-shadow:
    0 0 6px rgba(56, 189, 248, 0.9),
    0 0 14px rgba(56, 189, 248, 0.7);
  opacity: 0.9;
  transition: opacity 0.2s ease;
}




/* =========================
   SUPER MONOCHROME MEDICAL CRT
   - 색감 거의 100% 흑백
   - 미세한 딥블루 1%만 남김(육안상 거의 회색)
   - CRT 스캔 + 노이즈 유지
========================= */

/* ====== 전체 오버레이 ====== */
#dashboard-overlay {
  position: fixed;
  inset: 0;

  /* 진짜 흑백 기반 */
  background:
    radial-gradient(circle at top, rgba(0, 0, 0, 0.55), rgba(0, 0, 0, 0.75));
  backdrop-filter: blur(2px);
  -webkit-backdrop-filter: blur(2px);

  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

/* CRT 노이즈 */
#dashboard-overlay::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;

  background-image:
    repeating-linear-gradient(
      0deg,
      rgba(255, 255, 255, 0.03),
      rgba(255, 255, 255, 0.03) 1px,
      transparent 1px,
      transparent 3px
    ),
    repeating-linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.02),
      rgba(255, 255, 255, 0.02) 1px,
      transparent 1px,
      transparent 4px
    );
  opacity: 0.15;

  animation: noiseShift 0.6s steps(3, end) infinite;
}

.hidden {
  display: none !important;
}

/* ====== CRT 박스 ====== */
#dashboard-content {
  position: relative;
  width: min(1100px, 92vw);
  max-height: 80vh;
  border-radius: 12px;

  border: 2px solid rgba(50, 50, 50, 1);

  /* 완전 흑백 */
  background:
    repeating-linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.4) 0px,
      rgba(0, 0, 0, 0.4) 1px,
      rgba(0, 0, 0, 0.0) 1px,
      rgba(0, 0, 0, 0.0) 3px
    ),
    radial-gradient(circle at center, #050505 0%, #020202 55%, #000000 100%);

  box-shadow:
    0 0 0 4px rgba(0, 0, 0, 1),
    0 0 70px rgba(0, 0, 0, 1);

  padding: 20px 24px 18px;
  display: flex;
  flex-direction: column;
  gap: 16px;

  color: #f1f1f1;
}

/* 외곽 CRT 곡면 */
#dashboard-content::before {
  content: "";
  position: absolute;
  inset: -6px;
  border-radius: 18px;
  pointer-events: none;

  background:
    radial-gradient(circle at top left, rgba(255, 255, 255, 0.1), transparent 60%),
    radial-gradient(circle at bottom right, rgba(0, 0, 0, 0.7), transparent 60%);
  mix-blend-mode: screen;
  opacity: 0.8;
}

/* 좌→우 스캔 */
#dashboard-content::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  border-radius: 12px;

  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0) 0%,
    rgba(255, 255, 255, 0.16) 50%,
    rgba(255, 255, 255, 0) 100%
  );
  opacity: 0.16;
  mix-blend-mode: screen;

  background-size: 35% 100%;
  background-position: -40% 0;

  animation: crtSweep 4s linear infinite;
}

/* =========================
   상단 메타
========================= */

.dashboard-meta {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  font-family: "Orbitron", system-ui;
  letter-spacing: 0.18em;
  text-transform: uppercase;

  color: #f1f1f1;
  border-bottom: 1px solid rgba(120, 120, 120, 1);
  padding-bottom: 4px;

  text-shadow: 0 0 3px rgba(255, 255, 255, 0.3);
}

.dashboard-meta-label {
  font-size: 0.7rem;
  color: #bdbdbd;
}

#dashboard-node-name {
  font-size: 0.85rem;
  color: #ffffff;
}

/* =========================
   본문 그리드
========================= */

.dashboard-grid {
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  gap: 16px;
  min-height: 360px;
}

/* =========================
   LEFT : 사진 + 소개
========================= */

.dash-left {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* 의료 영상 패널 */
.dash-photo-panel {
  flex: 1;
  min-height: 220px;
  border-radius: 8px;
  overflow: hidden;
  border: 1px solid rgba(120, 120, 120, 1);
  background:
    repeating-linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.5) 0px,
      rgba(0, 0, 0, 0.5) 1px,
      transparent 1px,
      transparent 3px
    ),
    radial-gradient(circle at center, #141414, #050505);
  display: flex;
  align-items: center;
  justify-content: center;
}

#dashboard-photo {
  max-width: 100%;
  max-height: 100%;
  object-fit: cover;

  /* 거의 완전한 흑백 + 약간 contrast ↑ */
  filter: grayscale(1) contrast(1.1) brightness(0.95);
}

/* 텍스트 카드 */
.dash-text-panel {
  min-height: 80px;
  border-radius: 6px;
  padding: 10px 12px;
  border: 1px solid rgba(120, 120, 120, 1);
  background:
    repeating-linear-gradient(
      to bottom,
      rgba(10, 10, 10, 1) 0px,
      rgba(10, 10, 10, 1) 1px,
      rgba(15, 15, 15, 1) 1px,
      rgba(15, 15, 15, 1) 3px
    );
  color: #f1f1f1;
}

#dashboard-main-title {
  font-size: 0.85rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  margin-bottom: 4px;
  color: #e8e8e8;
}

#dashboard-main-text {
  font-size: 0.8rem;
  line-height: 1.5;
}

/* =========================
   RIGHT : 카드 3개
========================= */

.dash-right {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.dash-card {
  position: relative;
  border-radius: 6px;
  padding: 10px 12px;
  border: 1px solid rgba(120, 120, 120, 1);
  background:
    repeating-linear-gradient(
      to bottom,
      rgba(10, 10, 10, 1) 0px,
      rgba(10, 10, 10, 1) 1px,
      rgba(15, 15, 15, 1) 1px,
      rgba(15, 15, 15, 1) 3px
    );
  color: #f1f1f1;
  font-size: 0.8rem;
  text-shadow: 0 0 3px rgba(255, 255, 255, 0.2);
}

.dash-card::before {
  content: "";
  position: absolute;
  left: 10px;
  right: 10px;
  top: 0;
  height: 2px;
  background: linear-gradient(
    to right,
    rgba(200, 200, 200, 0.15),
    rgba(255, 255, 255, 0.7),
    rgba(200, 200, 200, 0.15)
  );
}

.dash-card-label {
  font-size: 0.65rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: #cfcfcf;
  margin-bottom: 4px;
}

.dash-card-title h3 {
  font-size: 0.9rem;
  margin-bottom: 6px;
}

/* 태그 */
.dash-node-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
}

.dash-node-tags .dash-tag {
  border-radius: 999px;
  padding: 2px 8px;
  border: 1px solid rgba(200, 200, 200, 1);
  font-size: 0.7rem;
  text-transform: uppercase;
  color: #f1f1f1;
  background: #111111;
}

/* 설명 텍스트 */
.dashboard-body {
  font-size: 0.78rem;
  line-height: 1.5;
  color: #f1f1f1;
}

/* =========================
   ORBIT (흑백 version)
========================= */

.dash-card-node {
  position: relative;
  padding: 10px 12px;
}

.node-orbit {
  position: relative;
  width: 100%;
  height: 200px;
  border-radius: 50%;
  border: 1px solid rgba(120, 120, 120, 1);

  background:
    repeating-linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.45) 0px,
      rgba(0, 0, 0, 0.45) 1px,
      transparent 1px,
      transparent 3px
    ),
    radial-gradient(circle at center, #0a0a0a 0%, #020202 75%);
  display: flex;
  align-items: center;
  justify-content: center;
}

.node-orbit-core {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 22px;
  height: 22px;
  transform: translate(-50%, -50%);
  border-radius: 999px;
  background: #ffffff;
  box-shadow:
    0 0 14px rgba(255, 255, 255, 1),
    0 0 28px rgba(180, 180, 180, 0.9);
}

.node-orbit-labels,
#dash-node-orbit-labels {
  position: absolute;
  inset: 0;
  z-index: 3;
}

.dash-orbit-label {
  position: absolute;
  min-width: 40px;
  padding: 4px 8px;
  border-radius: 999px;
  border: 1px solid rgba(200, 200, 200, 1);
  background: #121212;
  color: #f1f1f1;
  font-size: 10px;
  text-transform: uppercase;
  white-space: nowrap;
  cursor: pointer;

  transform: translate(-50%, -50%);
  pointer-events: auto;
  z-index: 4;

  transition:
    background 0.15s ease,
    color 0.15s ease,
    transform 0.15s ease,
    box-shadow 0.15s ease;
}

.dash-orbit-label:hover {
  transform: translate(-50%, -50%) scale(1.05);
  background: #ffffff;
  color: #000000;
  box-shadow: 0 0 10px rgba(255, 255, 255, 1);
}

.dash-orbit-label.active {
  background: #ffffff;
  color: #000000;
}

/* =========================
   닫기 버튼
========================= */

#close-dashboard {
  position: absolute;
  top: 10px;
  right: 12px;

  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid rgba(200, 200, 200, 1);
  background: #121212;
  color: #f1f1f1;

  font-size: 0.7rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;

  cursor: pointer;
  z-index: 99999;
  pointer-events: auto;
}

#close-dashboard:hover {
  background: #ffffff;
  border-color: #ffffff;
  color: #000000;
}

/* =========================
   CRT 애니메이션
========================= */

@keyframes noiseShift {
  0% { transform: translate(0, 0); }
  25% { transform: translate(-1px, 1px); }
  50% { transform: translate(1px, -1px); }
  75% { transform: translate(-1px, 0); }
  100% { transform: translate(0, 0); }
}

@keyframes crtSweep {
  0% { background-position: -40% 0; }
  100% { background-position: 140% 0; }
}
